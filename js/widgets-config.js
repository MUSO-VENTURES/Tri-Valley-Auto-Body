/* ============================================================
   Tri-Valley Auto Body — WIDGET CONFIG
   ------------------------------------------------------------
   This is the ONLY file you should need to edit to connect
   real accounts. Every value below is a placeholder. Until
   you fill one in, that widget shows a friendly "not connected
   yet" notice instead of a broken embed — nothing looks broken
   to a visitor in the meantime.

   See /README.md for step-by-step setup instructions for each
   service, including which are free.
   ============================================================ */

window.TVAB_CONFIG = {
  // Formspree (or Web3Forms) form endpoint for the Get an Estimate form.
  // Sign up free at https://formspree.io -> create a form -> paste its ID here.
  // Example real value: "mzbqjwpk"
  FORMSPREE_FORM_ID: "YOUR_FORMSPREE_ID",

  // Calendly (or Acuity) scheduling link for the booking widget.
  // Example real value: "https://calendly.com/tri-valley-auto-body/estimate"
  CALENDLY_URL: "YOUR_CALENDLY_URL",

  // Tawk.to live chat property + widget ID (free).
  // Found in Tawk.to dashboard -> Administration -> Channels -> Chat Widget.
  // Example real values: "60f1a2b3c4d5e6f7a8b9c0d1", "1f8g7h6j5"
  TAWKTO_PROPERTY_ID: "YOUR_TAWKTO_PROPERTY_ID",
  TAWKTO_WIDGET_ID: "YOUR_TAWKTO_WIDGET_ID",

  // Elfsight (or Trustindex) Google Reviews widget ID (free tier available).
  // Create a "Google Reviews" widget at https://elfsight.com and paste its ID.
  // Example real value: "3e9f2b41-....-....-....-............"
  GOOGLE_REVIEWS_WIDGET_ID: "YOUR_ELFSIGHT_WIDGET_ID",

  // Click-to-text number for the SMS fallback button (digits only, country code, no punctuation).
  SMS_NUMBER: "19254438548",

  // Shop phone (used in a couple of widgets for a "call us" fallback).
  SHOP_PHONE_DISPLAY: "925.443.8548",
  SHOP_PHONE_TEL: "19254438548",
};
